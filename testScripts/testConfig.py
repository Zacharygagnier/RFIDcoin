settings = {'database': 'database/coinData', # this is your database location
'inputDevice': '/dev/hidraw0', # this is where the input device is directly posting, use testinput to find out which
'outputDevice': '/dev/input/event1', # this is where the output device is sending  events to, use testOuput to find out which
'keyPress': 'BTN_SELECT', # this is the button that is pressed by the output device, device must have that key, refer to https://www.$
'startupCredits': '10' # add credits on restart, this number sets the credits to the alloted amount ON RESTART if they are under that$
}

